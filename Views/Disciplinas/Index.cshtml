@model IEnumerable<p2.Models.Disciplina>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h3 class="nomes">Disciplinas Cadastradas</h3>

<p>
    <a asp-action="Create" class="cor1">Criar Disciplina</a>
</p>
<table class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th scope="col">
                <div>@Html.DisplayNameFor(model => model.Descricao)</div><div class="th-pesquisar"><i class="fa fa-search"><input type="search" placeholder="Pesquisar..." /></i></div>
            </th>
            <th scope="col">
                Alterações:
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                <a asp-action="Edit" class="fa fa-pencil" asp-route-id="@item.Id"></a> |
                <a asp-action="Details" class="fa fa-info-circle" asp-route-id="@item.Id"></a> |
                <a asp-action="Delete" class="fa fa-trash" asp-route-id="@item.Id"></a>
            </td>
        </tr>
}
    </tbody>
</table>
